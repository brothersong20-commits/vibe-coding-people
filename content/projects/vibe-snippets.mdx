모임 멤버들이 자주 쓰는 코드 조각을 한 군데 모으기 위한 작은 보드. 처음에는 마크다운 파일 하나로 시작해도 충분하지만, 검색이 안 되는 순간 한계가 옵니다.

## 1차 목표

- 스니펫을 30초 안에 추가하고 30초 안에 찾는다.

## 데이터 모델

```ts
type Snippet = {
  id: string;
  title: string;
  language: "ts" | "tsx" | "py" | "sh" | "md";
  tags: string[];
  body: string;
  author: string;
  createdAt: string;
};
```

## 다음 단계

- 태그·언어 필터.
- 코드 블록에서 바로 복사하는 버튼(클립보드).
- "내가 추가한 스니펫만 보기" 뷰.
